#include <stdlib.h>
#include "calculos.h" 

double calcular_area_monte_carlo(double a,
                                         double b,
                                         int n,         // n de pontos no calculo da soma
                                         double (*fptr)(double)){
		seed48(7355608);
		double y =drand48();
        return y;
}
